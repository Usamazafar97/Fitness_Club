<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fitnessclub">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Feedback">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddTrainer" />
        <activity android:name=".AddTrainee" />
        <activity android:name=".AddTimeslot" />
        <activity android:name=".EditTrainer" />
        <activity android:name=".EditTrainee" />
        <activity android:name=".EditTimeslot" />
        <activity android:name=".TraineeLog">

        </activity>
        <activity android:name=".AdminLog" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>